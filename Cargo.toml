[package]
name = "version_renamer"
version = "0.3.0"
edition = "2024"

[dependencies]
windows = { version = "0.62.2", default-features = false, features = [
    "Win32_Storage_FileSystem",
    "Win32_System_WindowsProgramming",
    "Win32_System_ApplicationInstallationAndServicing",
] }
[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
